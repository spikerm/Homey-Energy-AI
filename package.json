{
  "name": "com.rexm.energyai",
  "version": "0.1.0",
  "description": "Advisory-first intelligent energy management for Homey Pro",
  "main": "app.js",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "validate": "npm run build && homey app validate",
    "test": "npm run build && eslint . --ext .ts"
  },
  "dependencies": {
    "homey": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
